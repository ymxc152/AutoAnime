# AutoAnime 修复任务 Agent 协作记忆

> 由主 Agent 维护，供审核器/执行器/反馈器接力阅读。内容力求精简，避免 token 浪费。

## 1. 任务目标
用 `F:\下载` 中真实动漫文件全面验证并修复识别错误；整理输出到 `F:\test`（**严禁移动到 `F:\动漫库`**）。最终让常见字幕组命名格式都能收敛到正确中文剧名。

## 2. 当前结论（已验证）
- OpenAI API 已过期，必须**临时**禁用：`config.ini` 中 `USEOPENAIAPI=False`、`NETERRRECTRYTIMS=0`，测试完恢复原值。
- Bangumi 网络不可达，TMDB token 未配置，因此实际走的是 `local_rules -> traditional_api(BGM/Bangumi/TMDB) -> local_rules_only` 回退链路。
- 第一轮全量 dry-run（747 个文件）结果：
  - 未收敛到中文目录：109 条
  - 全链路失败跳过：2 条
  - 高频英文目录：`Isekai-Nonbiri-Nouka-2`(12)、`Maid-san-wa-Taberu-Dake`(12)、`Otonari-no-Tenshi-sama-...-2`(10)、`Aishiteru-Game-wo-Owarasetai`(7)、`Ookii-Onnanoko-wa-Suki-Desuka`(7) 等。
- 已确认缓存中**没有**这些英文名的有效中文 canonical 映射（见 `F:/test/cache_existing_titles.log`），核心缺口是**手工白名单**。

## 3. 根因
- `autoanime/identification/local_fallback.py` 在 API 全失败后返回 `local_rules_only`，剧名就是文件名截断后的英文/罗马音 RAWName。
- `autoanime/cache/manual_whitelist.py` 内置默认条目太少，真实数据里大量英文名无映射。
- 用户指定要支持的两种「六四位元/今晚月色真美」格式，虽然含中文，但本地规则抽剧名时可能把后面的英文/日文混进去，需要白名单兜底。

## 4. 修复策略（已根据审核意见修订）
1. **扩展手工白名单**：把高频英文/罗马音别名 + 中文别译 + 低频英文名统一映射到正确中文剧名；默认写进 `manual_whitelist.py` 的 `DefaultWhitelist`。
2. **不修改 `Auxiliary_UniformOTSTR`**：保留全角 `？` 会引入中英粘连风险，改为白名单中文别名兜底。
3. **新增单元测试**：
   - `tests/test_manual_whitelist.py` 覆盖新增映射。
   - 新增用户指定格式集成测试（mock API 全失败）。
4. **清理错误缓存**：备份 `.cache/` 后，直接删除 `.cache/titles.json` 与 `.cache/organization.json`，再跑 dry-run 重建正确 canonical。
5. **回归验证**：重新全量 dry-run，目标未收敛中文目录 ≤ 10 条。
6. **TDD 小步迭代**：执行器每次只改一个小单元并跑测试，反馈器用真实数据验证后再继续下一单元。

## 5. 关键文件
- `autoanime/cache/manual_whitelist.py`：默认白名单。
- `autoanime/identification/local_fallback.py`：回退链路。
- `autoanime/text_utils.py`：`Auxiliary_CleanFallbackTitle` 等。
- `tests/test_manual_whitelist.py`：单元测试。
- `scripts/cache_doctor.py`：缓存纠偏工具。

## 6. 关键命令
```bash
# 全量 dry-run（测试前临时改 config.ini）
PYTHONIOENCODING=utf-8 .venv/Scripts/python AutoAnimeMv2.py "F:/下载" --output-path "F:/test" --dry-run > F:/test/dryrun_realdata_roundN.log

# 跑测试
PYTHONIOENCODING=utf-8 .venv/Scripts/python -m pytest tests/test_manual_whitelist.py -v

# 缓存检查
.venv/Scripts/python scripts/cache_doctor.py --inspect --cache-dir .\.cache
```

## 7. 待办状态
见主 Agent 的 `TodoList`。

## 8. 约束
- 不要永久修改 `config.ini`。
- 不要移动到 `F:\动漫库`。
- 所有代码改动需通过单元测试 + 真实数据 dry-run。
- git 提交使用中文 message。

## 9. 最终结果
- 白名单条目：从 12 条扩展到 64 条（含冗余清理后净增 52 条）。
- 全量 dry-run：`F:\下载` 978 个文件，未收敛中文目录 **0 条**，全链路失败跳过 **0 条**。
- 用户指定两种格式：单元测试验证均收敛到 `哪里有温柔对待阿宅的辣妹！？`。
- 全量测试：`88 passed`。
- 缓存：已备份 `.cache.bak.20260708_084545/`，并修复 `新石纪` 错误 alias。
- 提交：`5d9b705`（执行器首次提交），后续修正提交待完成。
- 配置：`config.ini` 已恢复原始值。

