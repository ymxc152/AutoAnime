# AI识别优先方案

## 功能背景
原有剧名标准化流程主要依赖 `Bangumi/BGM/TMDB`，在命名噪声较高或中文别名场景下存在识别不稳定问题。为提升识别覆盖与鲁棒性，引入 OpenAI 兼容接口作为首选识别通道。

## 功能目标
- 新增 AI 识别通道，默认优先执行。
- AI 失败时，若 `OPENAI_FALLBACK_ON_FAILURE=True`（默认）则回退到本地规则 + BGM/Bangumi/TMDB 传统 API；旧入口 `AutoAnimeMv.py` 无此回退，失败即退出。
- 密钥不落库：支持环境变量注入并对日志敏感值脱敏。
- 在启用 `OPENAI_IDENTIFY_ALL` 时，由 AI 直接输出剧名/季/集，减少本地规则误判。
- **OpenAI 请求体中的 `user` 内容**对文件基名先做 **`Auxiliary_StripLeadingBracketReleaseTags`**：去掉首部连续的半角 `[字幕组/发行标签]` 与全角 `【…】`，避免组名（如 `[Tsukigakirei]`）被误认为番名；内存缓存 key、`Auxiliary_PreDetectEpisodeHint` 等仍使用**原始 `path.basename`**，避免破坏已有缓存与集数提示。
- 在 AI 全信息识别场景中，同步输出英文名与罗马音，并通过别名索引复用历史中文主名称。
- 对 `episode=x.0` 等小数噪声做归一，避免普通剧集被误判到 `Season00`。
- 当 AI 结果与文件名历史别名冲突时，优先保留历史 canonical 映射，抑制“单集漂移”。
- AI 返回剧名线索后，统一走 **`Auxiliary_ResolvePlannedTitleChain`**：**TMDB 中文 → Bangumi → TMDB 英文 → OpenAI 译中文**；链失败则退出（无旧式 `try_apis` 多路轮询）。
- **季/集**优先来自 OpenAI 全信息；识别失败时新入口默认走回退链路，不回退本地截断推季集。

## 功能边界
- 默认改造“剧名标准化识别”链路；当 `OPENAI_IDENTIFY_ALL=True` 时也改造季集识别入口。
- 不新增第三方 Python 依赖。
- 不引入守护进程或服务端改造。

## 架构概览
1. 在 `Start_PATH()` 初始化新增 OpenAI 默认配置（`autoanime/cli.py`）。
2. 在 `Processing_Identification()`（`autoanime/identification/__init__.py`）中优先调用 `Auxiliary_OpenAIIdentifyFileInfo()`：
   - 调用 `POST {OPENAI_BASE_URL}/v1/chat/completions`；`messages` 中 `user` 使用剥除首部方括号标签后的基名；`system` 提示中说明方括号内多为字幕组/发行方。
   - 解析 JSON：`anime_name_zh/.../season/episode/...`
   - 成功后走 **`Auxiliary_ResolvePlannedTitleChain`**（`autoanime/identification/title_chain.py`）得中文主名与 `canonical_id`；OpenAI 识别结果 **仅内存缓存**（`OpenAIIdentifyFileMemoryCache`），不持久化 `OpenAIFileInfo`
   - 对 `episode` 做后处理（如 `8.0 -> 8`），仅在真实特典条件下落入 `Season00`
   - 发生“AI结果别名”和“文件名历史别名”冲突时，优先采用历史 canonical 映射（`Auxiliary_PreDetectEpisodeHint` 等）
3. **AI 失败回退与熔断**：
   - 若 `OPENAI_FALLBACK_ON_FAILURE=True`（默认），OpenAI 识别失败时由 `autoanime/identification/local_fallback.py` 接管：本地正则规则 → BGM → Bangumi → TMDB。
   - 连续 401/403/429/missing_key 失败达到 `OPENAI_FALLBACK_BREAKER_THRESHOLD`（默认 5）后触发熔断，后续文件直接跳过 AI、走回退链路。
4. 剧名链在缓存未命中时调用 **`Auxiliary_ResolvePlannedTitleChain`**（必要时内部使用 `OpenAIApi` 做译名）；不再使用历史 `try_apis` 顺序。
5. 在配置链路中新增：
   - `[Settings]` 与 `[#Config]` 兼容（`autoanime/config_loader.py`）
   - 配置值解析与敏感字段脱敏
6. 在接口层新增：
   - `Auxiliary_Http()`（`autoanime/apis/http.py`）统一 JSON 解析与状态码校验
   - API 结果支持内存缓存 + 持久化缓存（TTL，`autoanime/cache/persistent.py`）
   - 标题别名统一索引：`TitleAliasIndex` + `CanonicalTitleIndex`（`autoanime/cache/canonical.py`）

## 模块职责
| 模块 | 文件 | 职责 | 输入 | 输出 |
| --- | --- | --- | --- | --- |
| CLI 与初始化模块 | `autoanime/cli.py` (`Start_PATH` / `Start_GetArgv`) | 初始化 AI 开关、网关、模型、超时、优先级；解析参数 | 内置默认值 / CLI 参数 | 全局状态 + 运行上下文 |
| 配置读取模块 | `autoanime/config_loader.py` (`Auxiliary_READConfig` / `Auxiliary_ApplyConfig`) | 读取 `config.ini` 并兼容不同分区名；解析值并写入运行态 | 本地配置文件 | `ConfigMagdict` / 生效配置 |
| AI 文件识别模块 | `autoanime/identification/openai_identify.py` (`Auxiliary_OpenAIIdentifyFileInfo`) | 通过 OpenAI 兼容协议提取剧名/季/集，并优先用罗马音/英文查询 TMDB 回填中文 | 原始文件名 | `(SE,EP,RAWSE,RAWEP,RAWName)` + `NameEN/NameRomaji/CanonicalID` |
| AI 失败回退模块 | `autoanime/identification/local_fallback.py` | OpenAI 失败时的本地规则 + 传统 API 回退链路；熔断判定 | 原始文件名 / 失败原因 | 识别结果或 None |
| 剧名链模块 | `autoanime/identification/title_chain.py` (`Auxiliary_ResolvePlannedTitleChain`) | TMDB 中文→Bangumi→TMDB 英文→OpenAI 译中文 | 中英罗线索 | 中文主名或退出 |
| AI 客户端模块 | `autoanime/apis/openai_client.py` | OpenAI 兼容网关请求、多 slot 轮询、运行时状态持久化 | 提示词 / 模型 / key | 标准化标题/空 |
| 别名与主名索引模块 | `autoanime/cache/canonical.py` | 维护 `CanonicalTitleIndex` 与 `TitleAliasIndex` | 中英罗标题、来源 | `canonical_id` + 中文主名称 |

## 文件清单
| 文件路径 | 类型 | 作用 | 谁会使用它 | 它依赖谁 | 备注 |
| --- | --- | --- | --- | --- | --- |
| `AutoAnimeMv2.py` | Python | 推荐入口，调用 `autoanime.cli` | 用户 / 自动化 | `autoanime` | 新推荐入口 |
| `autoanime/cli.py` | 模块 | `Start_PATH` / `Start_GetArgv` / `main` | `AutoAnimeMv2.py` | `state`、`config_loader` | CLI 与初始化 |
| `autoanime/identification/__init__.py` | 模块 | `Processing_Identification`：识别编排（OpenAI 优先 + 回退分派） | `pipeline.main` | `openai_identify`、`local_fallback` | |
| `autoanime/identification/openai_identify.py` | 模块 | `Auxiliary_OpenAIIdentifyFileInfo` | `identification` | `apis/openai_client`、`naming` | |
| `autoanime/identification/local_fallback.py` | 模块 | AI 失败回退链路 + 熔断器 | `identification` | `naming`、各 API | 新入口独有 |
| `autoanime/identification/title_chain.py` | 模块 | 剧名链收敛 | `identification` / `openai_identify` | `apis/tmdb`、`apis/bangumi` | |
| `autoanime/apis/openai_client.py` | 模块 | OpenAI 兼容客户端、多 slot 轮询、译名 | `identification` | `requests` | |
| `autoanime/cache/canonical.py` | 模块 | `CanonicalTitleIndex` / `TitleAliasIndex` | `identification`、`pipeline` | `cache/persistent` | |
| `config.ini.Template` | 配置模板 | 给出 AI 配置和安全示例 | 部署者 | 配置解析逻辑 | 不含真实密钥 |
| `.gitignore` | Git 配置 | 防止 `config.ini/.env` 误提交 | 开发者 | Git | 安全防护 |

## 函数清单
| 函数/方法 | 所在文件 | 作用 | 输入 | 输出 | 调用方 | 被调对象 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `Processing_Identification` | `autoanime/identification/__init__.py` | 识别编排：OpenAI 优先，失败时决定是否回退 | 文件名等 | 识别结果 | `pipeline/main` | `openai_identify`、`local_fallback` | 新入口 |
| `Auxiliary_OpenAIIdentifyFileInfo` | `autoanime/identification/openai_identify.py` | 调用 AI 网关提取剧名/季/集 | 原始文件名 | `(SE,EP,...)` | `identification` | `openai_client`、`title_chain` | |
| `Auxiliary_ResolveFileInfoWithFallback` | `autoanime/identification/local_fallback.py` | OpenAI 失败后的回退链路 | 原始文件名 | 识别结果或 None | `identification` | `naming`、各 API | 新入口 |
| `Auxiliary_ShouldTripOpenAIBreaker` | `autoanime/identification/local_fallback.py` | 判定是否触发熔断 | 失败原因 | bool | `identification` | 无 | 新入口 |
| `Auxiliary_ResolvePlannedTitleChain` | `autoanime/identification/title_chain.py` | 剧名链收敛：TMDB→Bangumi→TMDB EN→OpenAI 译 | 中英罗线索 | 中文主名 | `openai_identify` | `apis/tmdb`、`apis/bangumi` | |
| `Auxiliary_OpenAIChatCompletionsPost` | `autoanime/apis/openai_client.py` | OpenAI 兼容网关请求 | 提示词 / 模型 | AI 返回 | `openai_identify` | `requests.post` | 多 slot 轮询 |
| `Auxiliary_OpenAITranslateForeignTitleToChinese` | `autoanime/apis/openai_client.py` | 外文剧名译中文 | 外文标题 | 中文标题或 None | `title_chain` | `requests.post` | |
| `Auxiliary_QueryTMDBChineseTitle` | `autoanime/apis/tmdb.py` | TMDB 查询中文标题 | 查询名等 | 中文标题或 None | `title_chain` / `openai_identify` | `Auxiliary_Http` | |
| `Auxiliary_UpsertCanonicalTitle` | `autoanime/cache/canonical.py` | 维护中文主名称与别名索引 | 中英罗标题 | `canonical_id` | `pipeline` / `identification` | `cache/persistent` | |
| `Auxiliary_NormalizeConfigSection` | `autoanime/config_loader.py` | 统一配置分区名 | `section_name` | 规范化分区名 | `Auxiliary_READConfig` | 无 | |
| `Auxiliary_ParseConfigValue` | `autoanime/config_loader.py` | 解析 bool/int/list/string | 原始字符串 | Python 值 | `Auxiliary_ApplyConfig` | `literal_eval` | |
| `Auxiliary_MaskConfigValue` | `autoanime/config_loader.py` | 脱敏敏感配置输出 | 配置名、配置值 | 脱敏结果 | `Auxiliary_ApplyConfig` | 正则匹配 | |
| `Auxiliary_StripLeadingBracketReleaseTags` | `autoanime/naming.py`（主实现）、`AutoAnimeMv.py`（兼容） | 去掉基名首部连续 `[…]`/`【…】` 标签 | 基名字符串 | 剥标签后的基名 | `openai_identify` | 无 | |

## 调用关系
- 主链路（新入口）：`Processing_Main -> Processing_Identification -> Auxiliary_OpenAIIdentifyFileInfo -> Auxiliary_ResolvePlannedTitleChain`
- 回退链路（新入口）：`Processing_Identification -> Auxiliary_OpenAIIdentifyFileInfo(失败) -> Auxiliary_ResolveFileInfoWithFallback -> 本地规则 / BGM / Bangumi / TMDB`
- 剧名链：`Auxiliary_ResolvePlannedTitleChain -> TMDB 中文 -> Bangumi -> TMDB 英文 -> OpenAI 译中文`（失败则退出）
- 缓存链路：`autoanime/cache/persistent.py -> API调用 -> 程序退出按子文件脏标志增量写盘`（索引组 + `ShowOrganizationIndex` 永不过期）
- 标题统一链路：`英文名/罗马音/线索 -> TitleAliasIndex/CanonicalTitleIndex`（由 `autoanime/cache/canonical.py` 维护）
- 配置链路：`Start_PATH -> Auxiliary_READConfig -> Auxiliary_ApplyConfig`

## 依赖关系
| 依赖项 | 类型 | 用途 | 入口 |
| --- | --- | --- | --- |
| `requests` | Python 库 | OpenAI/其他 API 请求 | `OpenAIApi` / `Auxiliary_Http` |
| LongCat OpenAI 兼容网关 | 外部服务 | AI 标题抽取 | `OpenAIApi` |
| 环境变量 `OPENAI_API_KEY` | 系统配置 | 安全注入密钥 | `OpenAIApi` |
| Schema v2 缓存子文件（`organization.json` / `titles.json` / `api_responses.json`） | 本地存储 | 降低重复 API 调用并统一多语言别名 | `autoanime/cache/persistent.py` |

## 配置说明
| 配置项 | 默认值 | 说明 |
| --- | --- | --- |
| `USEOPENAIAPI` | `True` | 是否启用 AI 识别 |
| `OPENAI_PRIORITY_FIRST` | `True` | 是否优先调用 AI |
| `OPENAI_IDENTIFY_ALL` | `True` | 是否由 AI 直接识别剧名/季/集 |
| `OPENAI_BASE_URL` | `https://api.longcat.chat/openai` | OpenAI 兼容网关地址 |
| `OPENAI_MODEL` | `LongCat-Flash-Chat` | 使用模型 |
| `OPENAI_TIMEOUT_SECONDS` | `60` | 请求超时秒数 |
| `OPENAI_API_KEY` | 空 | 可选本地直填，公开仓库不建议 |
| `OPENAI_API_KEY_ENV` | `OPENAI_API_KEY` | 读取 key 的环境变量名 |
| `CACHE_DIR` | `.cache` | 持久化缓存目录 |
| `CACHE_TTL_SECONDS` | `86400` | 缓存过期时间（秒） |

## 编译部署方法
- 本项目无需编译，按 Python 脚本直接运行。
- 部署步骤：
  1. 安装依赖：`python -m pip install -r requirements.txt`
  2. 复制模板：`config.ini.Template -> config.ini`
  3. 设置密钥环境变量：`OPENAI_API_KEY`
  4. 执行脚本：`python AutoAnimeMv2.py "目标目录"`

## 运行链路
1. 读取默认配置与本地配置（`autoanime/config_loader.py`）。
2. 扫描目录获取待处理文件（`autoanime/scanning.py`）；qB 回调时过滤 `.!qB` 等未完成下载临时文件。
3. 提取原始名称、季集信息。
4. 若 `OPENAI_IDENTIFY_ALL=True`，优先由 AI 识别剧名/季/集；失败且 `OPENAI_FALLBACK_ON_FAILURE=True`（默认）时回退本地规则 + 传统 API。
5. AI 全信息识别会优先使用 `anime_name_romaji/anime_name_en` 查询 TMDB 中文名称，未命中再回退 `anime_name_zh`。
6. 新入口 `AutoAnimeMv2.py` 下，AI 失败时默认走 `local_fallback` 链路；旧入口 `AutoAnimeMv.py` 无此回退，失败即退出。
7. 按 `SeasonXX/SXXEXX` 规则整理到目标目录。

## 调试方法
- 重点检查日志中的：
  - `OpenAIApi已启用,但未检测到可用密钥`
  - `OpenAIApi请求失败,状态码 ...`
  - `成功通过 OpenAI API获取到结果`
  - `OpenAI文件识别结果标题已按标准化缓存修正: 英文名 => 中文名`
  - `OpenAI文件识别已通过TMDB回填中文剧名`
  - `TMDB未命中中文剧名，回退使用OpenAI提供的中文名称`
  - `OpenAI识别结果与文件名别名冲突，优先保留历史别名`
  - `OpenAI文件识别缺少可用剧名`
  - `OpenAI 预失败...回退本地规则`（新入口回退日志，INFO 级别）
  - `OpenAI 熔断器已触发，后续文件跳过 AI`（连续失败后）
- 若 AI 连续失败，检查：
  - `OPENAI_API_KEY` 是否已设置
  - `OPENAI_BASE_URL` 与 `OPENAI_MODEL` 是否正确
  - 网络/代理是否可达
  - `OPENAI_FALLBACK_ON_FAILURE` 是否为 `True`（新入口）

## 验证结果
| 验证项 | 结果 | 说明 |
| --- | --- | --- |
| 代码改造完成 | 通过 | 已新增 AI 识别与优先级配置；新入口含回退链路与熔断机制 |
| 安全保护项 | 通过 | 模板不含真实 key/token，日志脱敏 |
| 配置兼容性 | 通过 | 支持 `[Settings]` 与 `[#Config]` |
| 本地回归测试 | 通过 | `unittest` 覆盖 API JSON 解析、缓存命中、回退链路逻辑 |
| 在线网关验证 | 通过 | 已使用真实 API key 验证 OpenAI 兼容接口可用 |
| AI 失败回退 | 通过 | `OPENAI_FALLBACK_ON_FAILURE=True` 时本地规则 + BGM/Bangumi/TMDB 回退可用 |
| 熔断机制 | 通过 | 连续 401/403/429/missing_key 达到阈值后跳过 AI |

## 风险点
- AI 网关或模型返回结构变化导致解析异常。
- 用户未配置 key 时会直接走回退 API，可能误判为 AI 不生效。
- 不同命名噪声下 AI 输出质量仍存在波动；若既无缓存又未按提示词返回中文名，仍可能落入外文标题。
- 别名索引纠偏策略若过激，可能导致中文名称在短期内频繁变动。
- 历史缓存若已写入错误别名，需要一轮新任务运行或定向清理对应缓存键，才能完全收敛到新策略。

## 已知问题
- 当前 AI 输出仅做基础清洗，复杂格式仍可能残留噪声。
- 未引入识别置信度字段，暂不支持多候选评分。

## 后续优化点
- 增加 AI 结果缓存失效策略。
- 增加可配置提示词模板与语言偏好。
- 增加“AI 结果与传统 API 交叉验证”策略。

## 变更记录
| 日期 | 修改来源 | 修改原因 | 影响范围 | 对应功能 / 文件 / 模块 |
| --- | --- | --- | --- | --- |
| 2026-04-06 | Agent | 新增 AI 识别优先方案并加强密钥保护 | API 识别链路、配置链路、安全策略 | `AutoAnimeMv.py` / `config.ini.Template` / `.gitignore` |
| 2026-04-06 | Agent | 接口解析改为 JSON 校验并接入持久化缓存 | API 识别链路、缓存链路 | `Auxiliary_Http` / `Auxiliary_Api` / `api_cache.json` |
| 2026-04-06 | Agent | 增加 OpenAI 全信息识别：由 AI 直接输出剧名/季/集 | 识别入口、命名链路、配置说明 | `Processing_Identification` / `Auxiliary_OpenAIIdentifyFileInfo` / `config.ini.Template` |
| 2026-04-07 | Agent | 修正 OpenAI 全信息识别剧名语言约束，并复用标题标准化缓存修正旧缓存英文名 | OpenAI 文件识别链路、缓存修正链路 | `Auxiliary_OpenAIIdentifyFileInfo` / `Auxiliary_GetStandardTitleFromCache` / `api_cache.json` |
| 2026-04-07 | Agent | 扩展 OpenAI 全信息识别返回英文名/罗马音并接入别名索引，支持外文别名复用历史中文主名称 | OpenAI 文件识别链路、标题统一链路、缓存结构 | `Auxiliary_OpenAIIdentifyFileInfo` / `Auxiliary_UpsertCanonicalTitle` / `TitleAliasIndex` / `CanonicalTitleIndex` |
| 2026-04-07 | Agent | 修复 `x.0` 剧集误判 `Season00`、单集剧名漂移和非中文剧名未二次标准化问题 | OpenAI 文件识别链路、主流程标准化链路、缓存收敛策略 | `Auxiliary_NormalizeEpisodeToken` / `Auxiliary_OpenAIIdentifyFileInfo` / `Processing_Main` |
| 2026-04-09 | Agent | 调整 AI 到 TMDB 中文回填策略：AI 识别后优先用罗马音/英文查询 TMDB，未命中才回退 AI 中文 | OpenAI 文件识别链路、TMDB 查询链路、主流程回退策略 | `Auxiliary_QueryTMDBChineseTitle` / `Auxiliary_OpenAIIdentifyFileInfo` / `Processing_Main` |
| 2026-04-16 | Agent | 剧名统一为 `Auxiliary_ResolvePlannedTitleChain`；OpenAI 识别仅内存缓存；`Auxiliary_Api` 不再走 `try_apis` | 识别、缓存、`api_cache.json` | `Auxiliary_ResolvePlannedTitleChain`、`Auxiliary_OpenAIIdentifyFileInfo` |
| 2026-04-22 | Agent | AI 主路径失败时默认走 `OPENAI_FALLBACK_ON_FAILURE`：本地规则 → BGM → Bangumi → TMDB；启用回退时 OpenAI 预失败日志降为 INFO 以减少误警 | 识别链路 | `autoanime/identification/__init__.py`、`local_fallback.py`、`openai_identify.py` |
| 2026-04-23 | Agent | OpenAI 全信息识别：`user` 提示使用 `Auxiliary_StripLeadingBracketReleaseTags` 剥除首部 `[…]`/`【…】`；`system` 补强组名非番名说明；实现见 `autoanime/naming.py` 与 `AutoAnimeMv.py` 同名辅助函数 | OpenAI 文件识别、命名辅助 | `Auxiliary_OpenAIIdentifyFileInfo`、`Auxiliary_StripLeadingBracketReleaseTags` |
| 2026-06-06 | Agent | 统一更新模块/函数位置指向 `autoanime/` 包；补充 `OPENAI_FALLBACK_ON_FAILURE`、熔断机制、回退链路说明；更新验证结果与调试方法 | 文档对齐 | `docs/06` |
